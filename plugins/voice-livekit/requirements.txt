# Wakey Voice Plugin - LiveKit Dependencies

# Core LiveKit Agents framework
livekit-agents>=0.12.0

# LiveKit plugins
livekit-plugins-deepgram>=0.10.0      # STT (Nova-3) + TTS (Aura-2)
livekit-plugins-openai>=0.10.0        # OpenAI-compatible LLM (for Groq)
livekit-plugins-silero>=0.10.0        # VAD (Voice Activity Detection)
livekit-plugins-turn-detector>=0.10.0 # Multilingual turn detection

# Note: Groq is accessed via OpenAI-compatible API, no separate plugin needed

# Optional: Cartesia TTS (alternative to Deepgram TTS)
# livekit-plugins-cartesia>=0.10.0

# Optional: BVC noise cancellation (if needed)
# livekit-plugins-noise-cancellation>=0.10.0